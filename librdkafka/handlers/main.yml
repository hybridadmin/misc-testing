---
# Handlers are not strictly needed for this role since apt_repository
# and apt modules handle cache updates inline. This file is kept as a
# placeholder for future use (e.g. restarting rsyslog after config changes).

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
