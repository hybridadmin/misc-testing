# =============================================================================
# pgBackRest Configuration Reference (pg_multi_pglogical)
# =============================================================================
# This file is NOT mounted into containers. It serves as documentation.
# The actual pgbackrest.conf is generated at runtime by pg-entrypoint.sh
# with the correct stanza name from the PGBACKREST_STANZA env var.
#
# Architecture:
#   3 independent PG nodes (multi-master pglogical replication + HAProxy)
#   Each node has its own system-id (independent initdb) -> 3 stanzas
#
# Stanzas:
#   pg-mmp-node1  -> mmp-pg-node1 (PG multi-master pglogical node 1)
#   pg-mmp-node2  -> mmp-pg-node2 (PG multi-master pglogical node 2)
#   pg-mmp-node3  -> mmp-pg-node3 (PG multi-master pglogical node 3)
#
# Shared volume:
#   pgbackrest-repo  -> /var/lib/pgbackrest (all 3 nodes)
#
# Per-node volumes:
#   pgbackrest-spool{N} -> /var/spool/pgbackrest (async archive spool)
#   pgbackrest-log{N}   -> /var/log/pgbackrest   (log files)
# =============================================================================

[pg-mmp-node1]
pg1-path=/var/lib/postgresql/data
pg1-port=5432
pg1-socket-path=/var/run/postgresql

[pg-mmp-node2]
pg1-path=/var/lib/postgresql/data
pg1-port=5432
pg1-socket-path=/var/run/postgresql

[pg-mmp-node3]
pg1-path=/var/lib/postgresql/data
pg1-port=5432
pg1-socket-path=/var/run/postgresql

[global]
repo1-path=/var/lib/pgbackrest
repo1-type=posix
repo1-retention-full=2
repo1-retention-diff=3
repo1-retention-archive=2
compress-type=lz4
compress-level=1
archive-async=y
spool-path=/var/spool/pgbackrest
start-fast=y
process-max=2
log-level-console=warn
log-path=/var/log/pgbackrest

[global:archive-push]
compress-level=3
log-level-console=info
